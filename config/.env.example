# Copy to .env (local dev) or set as machine-scope environment variables
# (Windows deployment, see windows/set_machine_env.ps1). Never commit real
# values — config files reference these names, never the secrets themselves.
NESSUS_ACCESS_KEY=
NESSUS_SECRET_KEY=
DRATA_API_KEY=
